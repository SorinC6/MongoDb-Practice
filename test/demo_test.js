//const mocha = require("mocha");
const assert = require("assert");

//Describe tests
describe("some demo tests", () => {
  //Create tests
  it("adds two numbers", () => {
    assert(2 + 3 === 5);
  });
});
